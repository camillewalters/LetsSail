using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testforkari : MonoBehaviour
{
    private void Awake()
    {
        
    }
    
    
    // Start is called before the first frame update
    void Start()
    {
     // time  = 0   
    }

    // Update is called once per frame
    void Update()
    {
        //time = 1
    }
}
